source "https://rubygems.org"

# GitHub Pages — usa esta gem para garantir compatibilidade com as
# versões que o GitHub usa no servidor.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end
